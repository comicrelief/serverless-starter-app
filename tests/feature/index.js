/**
 * Loads the integration tests
 * so that order can be defined.
 *
 * This is helpful since integration
 * tests should be executed
 * in a specified order.
 */

import '@/tests/feature/hello.feature';
